#include<stdio.h>
//esse era exemplo do slide mesmo, só olhei as variáveis e refiz, não tinha um enunciado.
int main() {
  int A = 5, B = 3;
  double media = (A+B) /2.0;
  printf("A = %d, B = %d, Média = %.2lf\n", A,B,media);
  return 0;
}
